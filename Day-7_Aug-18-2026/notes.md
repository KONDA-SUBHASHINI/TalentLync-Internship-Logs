# Day 7 - Started Javascript 

We can write javascript in either :
- Head section (In the script tag)
- body section (In the script tag)
- We can write javascript in .js file and include in your html file by using script tag - External JS.


### Explored datatypes, varaibles, functions, objects

Got knowledge on local storage and session storage
- Local storage stores data on the user browser.
- If the user changes browser ( say from chrome to incognito) then the local storage doesn't exist on new browser.

- Session storage loss data , even if we change new tab also. (Need to explore)

In local storage :
- We can save data using setItem()
- We can get data using getItem()